<div class="container h-100 mt-5 pt-5">
  <h2>Mis favoritos</h2>
  <div class="row row-cols-1 row-cols-md-2 g-4">
    {{#if items}}
      {{#each items}}
       {{>private/cardFavorite}}
      {{/each}}
    {{else}}
      <p>No se encontraron favoritos.</p>
    {{/if}}
  </div>

</div>